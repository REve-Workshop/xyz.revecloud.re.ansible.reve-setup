name: builder
comment: User for building software during host setup
state: all
root_repo_dir: codes
shell: /bin/bash
system: False
password: "{{ builder_pwd_from_vault }}"
# ---------------------------------
configurations:
  common_lisp:
    config: true
